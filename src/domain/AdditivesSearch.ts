export class AdditivesSearch {
  constructor(
    public additives: string,
    public additivesWord: string,
    public have_additives: string,
    public sake_name: string,
    public maker: string
  ) {}
}
